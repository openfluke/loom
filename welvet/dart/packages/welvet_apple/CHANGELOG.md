## 0.80.6

- macOS moved here from main `welvet`: `macos/welvet_apple.podspec` + `libwelvet.dylib`.
- Package now covers **iOS + macOS** (Apple platforms).

## 0.80.5

- Initial federated release: iOS Welvet.xcframework for `welvet`.
