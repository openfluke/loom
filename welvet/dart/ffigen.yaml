# Run with `dart run ffigen --config ffigen.yaml`.
name: WelvetBindings
description: |
  Bindings for `src/welvet.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/welvet_bindings_generated.dart'
headers:
  entry-points:
    - 'src/welvet.h'
  include-directives:
    - 'src/welvet.h'
comments:
  style: any
  length: full
