#include "textflag.h"

// float64 dotF32AccF64(x *float32, w *float32, n int) float64
TEXT ·dotF32AccF64(SB), NOSPLIT, $0-32
	MOVQ	x+0(FP), AX
	MOVQ	w+8(FP), BX
	MOVQ	n+16(FP), CX

	XORPD	X0, X0
	CMPQ	CX, $4
	JL	tail

loop4:
	MOVSS	(AX), X1
	MOVSS	4(AX), X2
	MOVSS	8(AX), X3
	MOVSS	12(AX), X4
	CVTSS2SD X1, X1
	CVTSS2SD X2, X2
	CVTSS2SD X3, X3
	CVTSS2SD X4, X4
	MOVSS	(BX), X5
	MOVSS	4(BX), X6
	MOVSS	8(BX), X7
	MOVSS	12(BX), X8
	CVTSS2SD X5, X5
	CVTSS2SD X6, X6
	CVTSS2SD X7, X7
	CVTSS2SD X8, X8
	MULSD	X5, X1
	MULSD	X6, X2
	MULSD	X7, X3
	MULSD	X8, X4
	ADDSD	X1, X0
	ADDSD	X2, X0
	ADDSD	X3, X0
	ADDSD	X4, X0
	ADDQ	$16, AX
	ADDQ	$16, BX
	SUBQ	$4, CX
	CMPQ	CX, $4
	JGE	loop4

tail:
	CMPQ	CX, $0
	JE	done
	MOVSS	(AX), X1
	CVTSS2SD X1, X1
	MOVSS	(BX), X2
	CVTSS2SD X2, X2
	MULSD	X2, X1
	ADDSD	X1, X0
	ADDQ	$4, AX
	ADDQ	$4, BX
	SUBQ	$1, CX
	JMP	tail

done:
	MOVSD	X0, ret+24(FP)
	RET
